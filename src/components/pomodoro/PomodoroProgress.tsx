import { CircularRoundProgress } from '@/components/pomodoro/CircularRoundProgress'

/**
 * Wrapper component for CircularRoundProgress
 * Maintains compatibility with existing usage in PomodoroTimer
 */
export function PomodoroProgress() {
  return <CircularRoundProgress />
}
